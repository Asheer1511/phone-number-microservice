/* eslint-disable prettier/prettier */
// phoneNumbers.interface.ts

export interface PhoneNumber {
  phoneNumber: string;
  countryCode: string;
  // Define other properties of the phone number as needed
}
